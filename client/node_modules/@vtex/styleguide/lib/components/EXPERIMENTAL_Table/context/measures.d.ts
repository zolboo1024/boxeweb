import React, { PropsWithChildren } from 'react';
import useTableMeasures from '../hooks/useTableMeasures';
declare type Measures = Partial<ReturnType<typeof useTableMeasures>>;
interface Props {
    measures: Measures;
}
export declare function useMeasuresContext(): Partial<{
    density: import("../hooks/useTableMeasures").Density;
    rowHeight: number;
    tableHeight: number;
    bodyHeight: number;
    setDensity: React.Dispatch<React.SetStateAction<import("../hooks/useTableMeasures").Density>>;
}>;
export declare function MeasuresProvider({ measures, children, }: PropsWithChildren<Props>): JSX.Element;
export {};
