import { PropsWithChildren } from 'react';
import { Column, Items } from '../types';
interface Data {
    columns: Column[];
    items: Items;
}
export declare function useDataContext(): Data;
export declare function DataProvider({ children, ...value }: PropsWithChildren<Data>): JSX.Element;
export {};
