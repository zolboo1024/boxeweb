import React, { PropsWithChildren } from 'react';
interface Loading {
    empty?: boolean;
    loading?: boolean | {
        renderAs?: () => React.ReactNode;
    };
    emptyState?: {
        label?: string;
        children?: Element;
    };
}
export declare function LoadingProvider({ children, ...value }: PropsWithChildren<Loading>): JSX.Element;
export declare function useLoadingContext(): Loading;
export {};
