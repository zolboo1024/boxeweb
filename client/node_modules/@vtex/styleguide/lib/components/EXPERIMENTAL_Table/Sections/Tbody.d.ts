import useTableMotion from '../hooks/useTableMotion';
import { ComposableRow } from './Row';
import { ComposableWithRef, RenderProps, NativeTableSection } from '../types';
interface BodyRenderProps {
    props: {
        data: unknown;
        motion: ReturnType<typeof useTableMotion>;
        height: number;
    };
    key: string;
    index: number;
}
declare type Props = RenderProps<NativeTableSection, BodyRenderProps>;
interface Composites {
    Row: ComposableRow;
}
export declare type ComposableTbody = ComposableWithRef<HTMLTableSectionElement, Props, Composites>;
declare const FowardedTbody: ComposableTbody;
export default FowardedTbody;
