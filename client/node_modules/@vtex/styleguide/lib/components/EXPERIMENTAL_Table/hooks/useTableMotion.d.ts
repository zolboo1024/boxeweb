export default function useTableMotion(transitions?: Transition[]): {};
declare type Transition = {
    prop: string;
    duration: number;
    func: string;
    delay: number;
    optimize?: boolean;
};
export {};
