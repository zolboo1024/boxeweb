"use strict";

exports.__esModule = true;
exports.getOneLevel = getOneLevel;
exports.getTwoLevels = getTwoLevels;
exports.getThreeLevels = getThreeLevels;
function getOneLevel(countryData) {
  return Object.keys(countryData);
}

function getTwoLevels(countryData) {
  return Object.keys(countryData).reduce(function (result, child) {
    var value = countryData[child];
    result[child] = Array.isArray(value) ? value : getOneLevel(value);
    return result;
  }, {});
}

function getThreeLevels(countryData) {
  return Object.keys(countryData).reduce(function (result, child) {
    result[child] = getTwoLevels(countryData[child]);
    return result;
  }, {});
}